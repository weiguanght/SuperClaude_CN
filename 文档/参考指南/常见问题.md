# SuperClaude 常见问题 —— 快速参考 🚀

**问题解决指南**：汇总最常见的问题及其对应的实际解决方案。

## 前五大快速修复方案 (解决 90% 的问题)

### 1. Claude Code 中命令无法工作 ⚡
- **现象**：输入 `/sc:brainstorm` 等命令无反应。
- **方案**：完全重启 Claude Code。
- **测试**：重新输入 `/sc:brainstorm "test"`，应当能看到引导提问。

### 2. 安装验证
```bash
python3 -m SuperClaude --version    # 应当显示 4.1.5

# 如果不起作用：
# 对于 pipx 用户
pipx upgrade SuperClaude

# 对于 pip 用户
pip install --upgrade SuperClaude

# 然后重新运行安装程序
python3 -m SuperClaude install
```

### 3. 权限问题
```bash
# 出现权限被拒绝 / PEP 668 错误：
# 选项 1：使用 pipx (强烈推荐)
pipx install SuperClaude

# 选项 2：使用 pip 命令并添加 --user 标志
pip install --user SuperClaude

# 选项 3：修复文件夹权限
sudo chown -R $USER ~/.claude
```

### 4. MCP 服务器问题
```bash
/sc:analyze . --no-mcp              # 尝试在不启用 MCP 服务器的情况下测试
node --version                      # 确认是否安装了 Node.js 16+ (如果需要)
```

### 5. 组件缺失
```bash
python3 -m SuperClaude install --components core commands agents modes --force
```

## 特定平台问题

**Windows:**
```cmd
set CLAUDE_CONFIG_DIR=%USERPROFILE%\.claude
python -m SuperClaude install --install-dir "%CLAUDE_CONFIG_DIR%"
```

**macOS:**
```bash
brew install python3 node
pip3 install SuperClaude
```

**Linux:**
```bash
sudo apt install python3 python3-pip nodejs
pip3 install SuperClaude
```

## 验证清单
- [ ] `python3 -m SuperClaude --version` 返回 4.1.5。
- [ ] 在 Claude Code 中执行 `/sc:brainstorm "test"` 正常工作。
- [ ] `SuperClaude install --list-components` 能正确显示各组件。

## 如果快速修复方案不起作用
请查阅 [疑难解答指南 (troubleshooting.md)](troubleshooting.md) 以获取深度诊断建议。
