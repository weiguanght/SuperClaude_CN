# SuperClaude 框架标志位 (Flags)

用于 Claude Code 的行为标志位，用于启用特定的执行模式和工具选择模式。

## 模式激活标志

**--brainstorm**
- 触发：模糊的项目请求、探索性关键词（如 "maybe", "thinking about", "not sure"）
- 行为：激活协作发现思维模式，提出探究性问题，引导需求挖掘。

**--introspect**
- 触发：自我分析请求、错误恢复、需要元认知能力的复杂问题解决。
- 行为：使用透明度标记（🤔, 🎯, ⚡, 📊, 💡）展示思考过程。

**--task-manage**
- 触发：多步操作（>3 步）、复杂范围（>2 个目录 或 >3 个文件）。
- 行为：通过授权、渐进式增强和系统的组织进行任务编排。

**--orchestrate**
- 触发：多工具操作、性能约束、并行执行机会。
- 行为：优化工具选择矩阵，启用并行思维，适应资源限制。

**--token-efficient**
- 触发：上下文使用率 >75%、大规模操作、带有 `--uc` 标志。
- 行为：使用符号增强通信，在保持清晰的同时减少 30-50% 的 token 占用。

## MCP 服务标志

**--c7 / --context7**
- 触发：库导入、框架相关问题、官方文档需求。
- 行为：启用 Context7 进行精选文档查找和模式指导。

**--seq / --sequential**
- 触发：复杂调试、系统设计、多组件分析。
- 行为：启用 Sequential 进行结构化的多步推理和假设检验。

**--magic**
- 触发：UI 组件请求（如 `/ui`, `/21`）、设计系统查询、前端开发。
- 行为：启用 Magic 基于 21st.dev 模式生成现代 UI。

**--morph / --morphllm**
- 触发：批量代码转换、基于模式的编辑、样式强制执行。
- 行为：启用 Morphllm 进行高效的多文件模式应用。

**--serena**
- 触发：符号操作、项目记忆需求、大型代码库导航。
- 行为：启用 Serena 进行语义理解和会话持久化。

**--play / --playwright**
- 触发：浏览器测试、端到端 (E2E) 场景、视觉验证、辅助功能测试。
- 行为：启用 Playwright 进行真实的浏览器自动化和测试。

**--chrome / --devtools**
- 触发：性能审核、调试、布局问题、网络分析、控制台错误。
- 行为：启用 Chrome DevTools 进行实时浏览器检查和性能分析。

**--tavily**
- 触发：Web 搜索请求、实时信息需求、研究查询、时事新闻。
- 行为：启用 Tavily 进行 Web 搜索和实时信息收集。

**--frontend-verify**
- 触发：UI 测试请求、前端调试、布局验证、组件验证。
- 行为：启用 Playwright + Chrome DevTools + Serena 进行全面的前端验证和调试。

**--all-mcp**
- 触发：最高复杂度场景、跨领域问题。
- 行为：启用所有 MCP 服务以获得最全面的能力。

**--no-mcp**
- 触发：仅限原生执行的需求、性能优先。
- 行为：禁用所有 MCP 服务，使用原生工具或 WebSearch 降级方案。

## 分析深度标志

**--think**
- 触发：多组件分析需求、中等复杂度。
- 行为：标准结构化分析（约 4K tokens），启用 Sequential。

**--think-hard**
- 触发：架构分析、全系统依赖分析。
- 行为：深度分析（约 10K tokens），启用 Sequential + Context7。

**--ultrathink**
- 触发：关键系统重构、遗留代码现代化、复杂调试。
- 行为：最大深度分析（约 32K tokens），启用所有 MCP 服务。

## 执行控制标志

**--delegate [auto|files|folders]**
- 触发：>7 个目录 或 >50 个文件 或 复杂度 >0.8。
- 行为：通过智能路由启用子智能体 (sub-agent) 并行处理。

**--concurrency [n]**
- 触发：资源优化需求、并行操作控制。
- 行为：控制最大并发操作数（范围：1-15）。

**--loop**
- 触发：改进类关键词（如 polish, refine, enhance, improve）。
- 行为：启用带有验证关卡的迭代改进周期。

**--iterations [n]**
- 触发：具体的改进周期要求。
- 行为：设置改进周期次数（范围：1-10）。

**--validate**
- 触发：风险评分 >0.7、资源使用 >75%、生产环境。
- 行为：执行前风险评估和验证关卡。

**--safe-mode**
- 触发：资源使用 >85%、生产环境、关键操作。
- 行为：最大程度验证、保守执行、自动启用 `--uc`。

## 输出优化标志

**--uc / --ultracompressed**
- 触发：上下文压力、效率要求、大型操作。
- 行为：符号通信系统，减少 30-50% 的 token 占用。

**--scope [file|module|project|system]**
- 触发：分析边界需求。
- 行为：定义运行范围和分析深度。

**--focus [performance|security|quality|architecture|accessibility|testing]**
- 触发：特定领域优化需求。
- 行为：针对特定分析领域和专业领域的应用。

## 标志优先级规则

**安全第一**：--safe-mode > --validate > 优化类标志
**显式覆盖**：用户手动设置的标志 > 自动检测
**深度等级**：--ultrathink > --think-hard > --think
**MCP 控制**：--no-mcp 覆盖所有单独的 MCP 标志
**作用域优先级**：system > project > module > file
