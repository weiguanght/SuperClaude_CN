# 📦 SuperClaude 安装指南

### **为您的 Claude Code 注入 21 条命令、14 们专家及 6 处 MCP 服务器的能力**

---

## ⚡ **快速安装**

您可以根据您的习惯选择以下任意一种安装方式：

| 方式 | 命令 | 平台 | 适用对象 |
|:------:|---------|:--------:|----------|
| **🐍 pipx** | `pipx install SuperClaude && SuperClaude install` | Linux/macOS | **✅ 推荐** - 环境隔离 |
| **📦 pip** | `pip install SuperClaude && SuperClaude install` | 所有 | 传统 Python 用户 |
| **🌐 npm** | `npm install -g @bifrost_inc/superclaude && superclaude install` | 所有 | Node.js 开发者 |
| **🔧 源码** | `git clone ... && pip install -e ".[dev]"` | 所有 | 贡献者与开发者 |

---

## 📋 **环境要求**

- **Python**: 3.8+ (`python3 --version`)
- **pip**: 最新版本 (`pip --version`)
- **Claude Code**: 已安装并可运行 (`claude --version`)
- **Node.js** (可选): 如需使用 MCP 服务器功能。

---

## ✅ **验证安装**

1. **核验版本号**：运行 `python3 -m SuperClaude --version`，应显示 `SuperClaude 4.1.5`。
2. **在 Claude Code 中测试**：
   - 输入 `/sc:brainstorm "测试项目"`：应触发交互式需求调研。
   - 输入 `@agent-security "检查代码"`：应激活安全专家。

---

## 🛠️ **常用管理命令**

- **更新**：`pip install --upgrade SuperClaude` 后运行 `SuperClaude update`。
- **备份**：`SuperClaude backup --create`。
- **卸载**：`SuperClaude uninstall`。

---

## 🔧 **故障排除 (PEP 668 错误)**
如果遇到权限或环境管理错误，请优先尝试使用 `pipx` 方式安装，或在 `pip` 命令后加上 `--user` 参数。

---

### **🎉 安装完成！**

您现在已经拥有了 21 条高效命令与 14 位 AI 专家的协助。现在就去 Claude Code 中输入 `/sc:brainstorm` 开启您的第一个 SuperClaude 项目吧！
