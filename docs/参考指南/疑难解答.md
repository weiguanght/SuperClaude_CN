# SuperClaude 疑难解答指南 (Troubleshooting Guide) 🔧

从快速修复到高级诊断，解决 SuperClaude 框架使用中的各种问题。

## 快速修复 (解决 90% 的问题)

**安装验证：**
```bash
python3 -m SuperClaude --version    # 应当显示 4.1.5
SuperClaude install --list-components
```

**命令执行问题：**
- 在 Claude Code 中测试：运行 `/sc:brainstorm "测试项目"`，应当能看到引导提问。
- **如果没有响应**：请完全重启 Claude Code 会话。

**自检清单：**
- [ ] 版本命令 `python3 -m SuperClaude --version` 正常工作并显示 4.1.5。
- [ ] 在 Claude Code 中输入 `/sc:` 命令能获得响应。
- [ ] 确认安装了 MCP 服务器：`SuperClaude install --list-components | grep mcp`。

## 常见问题

### 安装类问题

**包安装失败：**
```bash
# 对于 pipx 用户
pipx uninstall SuperClaude
pipx install SuperClaude

# 对于 pip 用户
pip uninstall SuperClaude
pip install --upgrade pip
pip install SuperClaude
```

**权限被拒绝 / PEP 668 错误：**
```bash
# 选项 1：使用 pipx (强烈推荐)
pipx install SuperClaude

# 选项 2：使用 pip 命令并添加 --user 标志
pip install --user SuperClaude

# 选项 3：修复文件夹权限
sudo chown -R $USER ~/.claude

# 选项 4：强制安装 (需谨慎使用)
pip install --break-system-packages SuperClaude
```

**核心组件缺失：**
```bash
python3 -m SuperClaude install --components core commands agents modes --force
```

### 命令类问题

**命令无法识别：**
1. 完全重启 Claude Code。
2. 通过 `python3 -m SuperClaude --version` 验证安装。
3. 测试运行 `/sc:brainstorm "test"`。

**智能体 (Agents) 未自动激活：**
- 确保关键词匹配：例如使用 `/sc:implement "实现安全的 JWT 认证"`。
- 手动激活：使用 `@agent-security "审查鉴权代码"`。

**性能缓慢：**
```bash
/sc:analyze . --no-mcp               # 在不启用 MCP 服务器的情况下测试
/sc:analyze src/ --scope file        # 缩小分析范围
```

### MCP 服务器类问题

**服务器连接失败：**
```bash
ls ~/.claude/.claude.json            # 确认配置文件是否存在
node --version                       # 确认 Node.js 版本为 16+
SuperClaude install --components mcp --force
```

**缺少 API Key (如 Magic/Morphllm)：**
```bash
export TWENTYFIRST_API_KEY="您的_KEY"
export MORPH_API_KEY="您的_KEY"
# 或者使用临时方案：/sc:命令 --no-mcp
```

## 高级诊断

**系统分析：**
```bash
SuperClaude install --diagnose
cat ~/.claude/logs/superclaude.log | tail -50
```

**组件完整性分析：**
```bash
ls -la ~/.claude/                    # 检查已安装的文件
grep -r "@" ~/.claude/CLAUDE.md      # 验证导入路径
```

**重置安装：**
```bash
SuperClaude backup --create          # 先创建备份
SuperClaude uninstall
SuperClaude install --fresh
```

## 获取帮助

**参考文档：**
- [安装指南](../Getting-Started/installation.md) —— 处理安装相关问题。
- [命令指南](../User-Guide/commands.md) —— 处理使用相关问题。

**社区支持：**
- [GitHub Issues](https://github.com/SuperClaude-Org/SuperClaude_Framework/issues)
- 提交时请包含：操作系统、Python 版本、具体的错误消息以及复现步骤。
