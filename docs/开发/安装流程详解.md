# SuperClaude 安装流程详解 (Installation Flow)

> **学习目标**: 完全理解安装程序是如何将文件配置到 `~/.claude/` 目录下的。

---

## 🔄 安装流程全景图

### 用户操作
```bash
# 步骤 1: 安装包
pipx install SuperClaude
# 或
npm install -g @bifrost_inc/superclaude

# 步骤 2: 执行设置
SuperClaude install
```

### 内部处理流程

1. **入口点 (Entry Point)**: `superclaude/__main__.py` 的 `main()` 函数。
2. **CLI 解析器**: 注册 `install` 子命令。
3. **组件管理器**: 在 `setup/cli/install.py` 中协调各安装组件。
4. **命令组件 (Commands Component)**: `setup/components/commands.py` 中的 `CommandsComponent` 类负责安装斜杠命令。
5. **源文件**: 位于 `superclaude/commands/*.md`。
6. **安装目标**: 将文件复制到 `~/.claude/commands/sc/*.md`。

---

## 📁 CommandsComponent 详解

### 核心逻辑
该组件负责斜杠命令的安装与管理。

- **安装路径**: 固定为用户环境下的 `~/.claude/commands/sc/`。
- **源码定位**: 通过计算项目根目录，定位到源码包中的 `superclaude/commands/` 文件夹。

### 关键映射关系
```
源码位置: ~/github/SuperClaude_Framework/superclaude/commands/*.md
目标位置: ~/.claude/commands/sc/*.md
```
例如，`superclaude/commands/pm.md` 会被复制为 `~/.claude/commands/sc/pm.md`。

---

## 📋 实际文件映射

### 源码 (本项目中)
- `superclaude/commands/pm.md` (PM 智能体定义)
- `superclaude/commands/implement.md` (实施命令)
- ... (共 26 个命令)

### 目标 (用户环境中)
- `~/.claude/commands/sc/pm.md` → 之后可通过 `/sc:pm` 执行。
- `~/.claude/commands/sc/implement.md` → 之后可通过 `/sc:implement` 执行。

### Claude Code 运行机制
1. 用户输入 `/sc:pm`。
2. Claude Code 读取 `~/.claude/commands/sc/pm.md`。
3. 解析 YAML 元数据并展开 Markdown 正文。
4. 作为 PM 智能体开始执行任务。

---

## 🔧 其他组件

- **模式组件 (Modes)**: 将 `superclaude/modes/*.md` 安装到 `~/.claude/*.md`。
- **智能体组件 (Agents)**: 将 `superclaude/agents/*.md` 安装到 `~/.claude/agents/*.md`。
- **核心组件 (Core)**: 将 `superclaude/core/CLAUDE.md` 安装到 `~/.claude/CLAUDE.md`（这是全局设置的来源）。

---

## 💡 开发时的注意事项

### ✅ 正确的修改流程
1. **修改源码文件**（受 Git 管理）：修改 `superclaude/commands/pm.md`。
2. **编写并运行测试**：在 `tests/` 目录下进行验证。
3. **提交变更**：`git commit` 提交代码。
4. **安装开发版进行验证**：运行 `SuperClaude install --dev`。

### ❌ 错误的方法
- **直接修改 `~/.claude/` 下的文件**：这些文件不受 Git 追踪，且在下次运行安装命令时会被源码覆盖，导致修改丢失。

---

## 🎯 PM 模式改进的标准工作流

1. **理解阶段**: 掌握安装流程与组件逻辑。
2. **现状确认**: 阅读源码 (`superclaude/commands/pm.md`)。
3. **编写提案**: 在本项目内编写假说文档 (`docs/Development/hypothesis-*.md`)。
4. **实施与测试**: 修改源码，编写单元测试，并使用开发版进行实际验证。
5. **记录学习**: 将成功经验记录到 `docs/patterns/`。

---

通过这篇文档，我们建立起了对**安装流程的完整理解记录**，避免在后续沟通中重复解释这些底层逻辑。
